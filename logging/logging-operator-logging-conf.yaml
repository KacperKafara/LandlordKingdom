clusterOutputs:
  - name: elasticsearch
    spec:
      elasticsearch:
        host: elasticsearch.logging.svc.cluster.local
        port: 9200
        scheme: http
        logstash_format: false
        logstash_prefix: kind
clusterFlows:
  - name: all-pods
    spec:
      globalOutputRefs:
        - elasticsearch