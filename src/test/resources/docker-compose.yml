version: '3.8'

services:
  db:
    image: postgres:16.2
    container_name: ssbd02-db
    environment:
      POSTGRES_DB: ssbd02
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"